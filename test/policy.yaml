apiVersion: policy.authentik.crossplane.io/v1alpha1
kind: Binding
metadata:
  name: binding
spec:
  forProvider:
    target: grafana
    group: cluster-admins
    enabled: true
    order: 0
  providerConfigRef:
    name: authentik-provider